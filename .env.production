# Production Environment Configuration
# =============================================
# This file contains all configuration for production deployment
# Keep this file secure and never commit to git!

# Django Settings
DEBUG=False
SECRET_KEY='k8f@vn2^jx-y4wm9$p!h3zqa(c+b7=e6rt*u_o1d&i5sg0lw%'
ALLOWED_HOSTS=localhost,127.0.0.1,backend

# Database Configuration
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=bsu_db
MYSQL_USER=bsu_user
MYSQL_PASSWORD=bsu_password

# Backend Database Connection
DB_NAME=bsu_db
DB_USER=bsu_user
DB_PASSWORD=bsu_password
DB_HOST=db
DB_PORT=3306

# CORS Configuration (comma-separated list)
CORS_ALLOWED_ORIGINS=http://localhost:8081,http://127.0.0.1:8081,http://localhost,http://127.0.0.1

# CSRF Configuration (comma-separated list)  
CSRF_TRUSTED_ORIGINS=http://localhost:8081,http://127.0.0.1:8081,http://localhost,http://127.0.0.1
